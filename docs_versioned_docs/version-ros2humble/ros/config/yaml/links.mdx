---
title: Links
sidebar_label: Links
sidebar_position: 5
toc_min_heading_level: 2
toc_max_heading_level: 4
---
Links are any URDF primitive.
> Just as is the case in URDF, links are added at the center of the primitive. In other words, you will need to slide up all links by half their height to have it "sit" on its parent link.

## Frame
Just a link without any geometry.
<table>
    <tr>
        <td>
            <center>
            <figure>
                <img
                    src={require("../img/links/frame.png").default}
                    width="250"
                />
            </figure>
            </center>
        </td>
<td>

```yaml
frame:
  - name: link_name
    parent: base_link
    xyz: [0.0, 0.0, 0.0]
    rpy: [0.0, 0.0, 0.0]
```
</td>
</tr>
</table>

## Box
A box of defined size.
<table>
    <tr>
        <td>
            <center>
            <figure>
                <img
                    src={require("../img/links/box.png").default}
                    width="250"
                />
            </figure>
            </center>
        </td>
<td>

```yaml
box:
  - name: box_name
    parent: base_link
    xyz: [0.0, 0.0, 0.0]
    rpy: [0.0, 0.0, 0.0]
    size: [0.01, 0.01, 0.01]
```
</td>
</tr>
</table>

## Cylinder
A cylinder of defined height and radius.
<table>
    <tr>
        <td>
            <center>
            <figure>
                <img
                    src={require("../img/links/cylinder.png").default}
                    width="250"
                />
            </figure>
            </center>
        </td>
<td>

```yaml
cylinder:
  - name: cylinder_name
    parent: base_link
    xyz: [0.0, 0.0, 0.0]
    rpy: [0.0, 0.0, 0.0]
    radius: 0.01
    length: 0.01
```
</td>
</tr>
</table>

## Sphere
A sphere of defined radius.
<table>
    <tr>
        <td>
            <center>
            <figure>
                <img
                    src={require("../img/links/sphere.png").default}
                    width="250"
                />
            </figure>
            </center>
        </td>
<td>

```yaml
sphere:
  - name: sphere_name
    parent: base_link
    xyz: [0.0, 0.0, 0.0]
    rpy: [0.0, 0.0, 0.0]
    radius: 0.01
```
</td>
</tr>
</table>

## Mesh
Include your custom mesh without creating a URDF of your own.

If you prefer to add a URDF instead, then you can link it directly through the `platform.extras` entry.


<table>
    <tr>
        <td>
            <center>
              Your Custom Mesh
            </center>
        </td>
<td>

```yaml
mesh:
  - name: mesh_name
    parent: base_link
    xyz: [0.0, 0.0, 0.0]
    rpy: [0.0, 0.0, 0.0]
    visual: /absolute/path/to/mesh.stl
```
</td>
</tr>
</table>



# Sample

<details><summary>Sample A200 Links Section</summary>
<p>

<center>
  <figure>
    <img
      src={require("../img/husky_platform_2.png").default}
      width="500"
    />
    <figcaption>Husky A200 with Open User Bay</figcaption>
  </figure>
</center>

In this sample, we will use links to add thin **box** to cover up the user bay.

We add an entry under the **box** list:

```yaml
box:
  - name: user_bay_cover
    parent: top_plate_link # add it to the top_plate_link
    xyz: [0.0, 0.0, 0.00735] # move it up 6.35mm (thickness of top plate) and move it up 1mm (half height of the box)
    rpy: [0.0, 0.0, 0.0]
    size: [0.4, 0.4, 0.002] # length 400mm, width 400mm and 2mm thick.
```

<center>
  <figure>
    <img
      src={require("../img/husky_accessories_0.png").default}
      width="500"
    />
    <figcaption>Husky A200 with Covered User Bay</figcaption>
  </figure>
</center>

We leave the rest of the lists all empty, for the resulting links section:

```yaml
links:
  box:
    - name: user_bay_cover
      parent: top_plate_link
      xyz: [0.0, 0.0, 0.00735]
      rpy: [0.0, 0.0, 0.0]
      size: [0.4, 0.4, 0.002]
  cylinder: []
  frame: []
  mesh: []
  sphere: []
```

</p>
</details>
